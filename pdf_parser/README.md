# Pdf to excel converter

A Python automation script that generates formatted Excel reports from pdfs.

## Requirements
- Python 3.x
- openpyxl library
- pdfplumber

## How to Use

Install the required library:
```
pip install openpyxl
```
pip install pdfplumber
```

Run the script:
```
python excel_generator.py